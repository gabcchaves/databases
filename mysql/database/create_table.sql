CREATE TABLE foo(
	id int,
	name varchar(10)
);

CREATE TABLE bar(
	id int,
	name varchar(10)
);

CREATE TABLE baz(
	id int,
	name varchar(10)
);

CREATE TABLE foobar(
	id int,
	name varchar(10)
);

CREATE TABLE foobaz(
	id int,
	name varchar(10)
);
